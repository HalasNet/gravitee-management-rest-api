title: New Support Ticket
message: >
  "${user.displayName}" has created a ticket.
  <#if api??> The affected API is ${api.name}.</#if>
  <#if application??> The affected application is ${application.name}.</#if>
